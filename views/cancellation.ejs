<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/CSS/cancel.css">
    <title>Cancellation Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body>
    

    <div class="cancellation-container">
        <h2>Cancellation Form</h2>
        <form id="cancellationForm">
            <div class="input-group offset-2 mt-3" >
                <label for="reservationId" >Reservation ID</label>
                <input type="text" id="reservationId" required>
            </div>
            <button type="submit" class="btn btn-danger col-6 offset-3 mt-5">Cancel Reservation</button>
        </form>
    </div>

    <!-- <script src="/JS/login.js"></script>
    <script>
        document.getElementById('loginForm').addEventListener('submit', function(event) {
            event.preventDefault(); // Prevent form submission
            
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value.trim();
            const errorMessage = document.getElementById('errorMessage');
            const cancellationContainer = document.querySelector('.cancellation-container');

            if (username === 'admin' && password === 'password123') {
                alert('Login successful!');
                errorMessage.style.display = 'none';
                cancellationContainer.style.display = 'block'; // Show cancellation form
            } else {
                errorMessage.textContent = 'Invalid username or password.';
                errorMessage.style.display = 'block';
            }
        });
    </script> -->
</body>
</html>